import React from "react";

function PluginButton({ name }) {
    return (
        <button
            className="plugin-button"
        >
            {name}
        </button>
    );
}

export default PluginButton